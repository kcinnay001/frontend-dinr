import React from 'react'
import { withRouter } from 'react-router-dom'

const Profile = () => {
 return <div> if you see this, it means your authenticated </div>
}

export default withRouter(Profile);
